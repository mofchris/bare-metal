# DECISIONS.md — numbered record of every non-trivial decision

Format (defined in BUILD_PLAN.md):

> **D-NNN** | Stage X | _One-line summary._
> Options considered: ... Tradeoff: ... My question to Christopher: none / [question].
> Status: pending / ratified at Gate X.

Decisions are appended, never rewritten. If a decision is reversed later, a new
entry references the old one.

---

**D-001** | Stage 0 | _Deployment: fully static, offline-capable PWA on GitHub Pages; progress in IndexedDB; labs run locally via a lab-runner in this repo; simulators run in-browser._
Options considered: this architecture (pre-made in BUILD_PLAN.md, validated here); Cloudflare Pages/Workers (adds an account + platform dependency, sync needs a server — deferred, see D-002); self-hosted server (needs always-on hardware and money, kills offline-first). Tradeoff: no cross-device sync in v1 — the one-file export/backup is the bridge between laptop and phone. Validation: the proposed stack (D-003…D-008) produces plain static files at build time; nothing at runtime needs a server. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-002** | Stage 0 | _Cloudflare Worker progress sync: considered and deferred to Stage E+._
Options considered: build sync now (real cross-device continuity, but needs auth, a backend, and conflict resolution — weeks of work before any studying happens); defer (export/backup file covers the risk at near-zero cost). Tradeoff: manually moving a backup file between devices is friction, but v1's job is studying, not infrastructure. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-003** | Stage 0 | _Language: TypeScript, compiled at build time (no server at runtime)._
Options considered: plain JavaScript (zero build step, but a data-heavy app — curriculum schemas, progress records, spaced-repetition state — loses too many errors to runtime); TypeScript (types document the data model and fail loudly at compile time; also a portfolio signal). Tradeoff: requires a build step, but GitHub Pages is fed by a build anyway (see D-006). My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-004** | Stage 0 | _UI framework: Preact (~4 KB) instead of React or vanilla DOM code._
Options considered: React (industry standard but ~45 KB and nothing in this app needs it over Preact); vanilla TS (zero deps, but the quiz engine + dashboard have enough UI state that hand-rolled DOM updates become the clever-dense code CLAUDE.md bans); Svelte (good fit technically, but compiler-generated output is harder for a stranger to map back to source); Preact (React's exact component model at 1/10 the size — readable to any reviewer who knows React, fits the 2 s cold-start budget). Tradeoff: slightly smaller ecosystem than React; acceptable because dependencies are liabilities here anyway. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-005** | Stage 0 | _Curriculum authored as Markdown + YAML frontmatter, compiled to JSON at build time._
Options considered: author directly in JSON (machine-friendly, miserable for humans writing lessons); parse Markdown in the browser at runtime (ships a parser to the client, and malformed content fails at study time — silently or late); compile at build time (authoring stays pleasant, the compiler validates every lesson/question and fails the build loudly on malformed content, runtime just loads pre-validated JSON). Tradeoff: content changes need a rebuild — fine, content ships with the app anyway. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-006** | Stage 0 | _Build tool: Vite; tests: Vitest; deploy: GitHub Actions → GitHub Pages._
Options considered: no bundler (ES modules straight to Pages — possible, but no TS, no content compilation, no service-worker precache manifest generation); Webpack (does everything, config burden not worth it); Vite (near-zero config, first-class TS, Vitest shares its config so tests need no separate setup). Tradeoff: Vite is a dev-time dependency only — nothing of it runs at runtime, so static hosting stays valid. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-007** | Stage 0 | _Progress storage: IndexedDB via the `idb` wrapper (~1 KB)._
Options considered: localStorage (synchronous, ~5 MB cap, string-only — too small once quiz history grows); raw IndexedDB API (no dependency, but its callback/event API is notoriously verbose and error-prone — exactly where silent bugs live); `idb` (thin promise wrapper, ~1 KB, well-maintained, keeps our code readable). Dependency justification per CLAUDE.md: tiny, boring, maintained by a Chrome team member, replaceable in an afternoon. Tradeoff: one more dependency; accepted for correctness. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-008** | Stage 0 | _Service worker: hand-rolled (~100 lines), not Workbox._
Options considered: Workbox (Google's library — robust, but a black box that defeats the BUILD_PLAN requirement that Christopher understand how offline caching works); hand-rolled precache-on-install + cache-first worker with a build-generated asset manifest (small, fully readable, and itself a systems lesson). Tradeoff: we own the cache-versioning bugs; mitigated by keeping the worker dead simple and testing offline behavior at Gate A. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19).

**D-009** | Stage 0 | _Lab-runner: Python + PyTorch (CPU), reporting results as a signed-ish JSON token the web app imports._
Options considered: JavaScript labs in-browser (no install friction, but MLSys labs need NumPy/PyTorch and real OS-level timing — the browser hides exactly what we're studying); Python lab-runner (the actual tools of the field, runs on the laptop where measurement is honest). Result hand-off: small JSON results file the app imports (no server, works offline). Tradeoff: Windows Python setup friction — mitigated by a documented one-time setup in Stage C. My question to Christopher: none.
Status: ratified at Gate 0 (2026-07-19); detailed design deferred to Stage C.

**D-011** | Stage 0 | _Progress sync will be a NEW Cloudflare Worker purpose-built for Metal — not a reuse of the existing study-sync Worker. Timing: still Stage E+, not v1._ (Refines D-002; also corrects it: D-002 implied no sync infrastructure existed, but `study-sync.mofchris.workers.dev` already serves the GRE and Network+ sims.)
Options considered: reuse study-sync as-is (fastest, proven auth + merge, but its model is "one localStorage blob per app" — Metal's IndexedDB stores with append-only attempts, SRS state, and lab results don't fit a single-blob PUT without lossy flattening); extend study-sync (couples three apps to one worker — a Metal schema change could break the exam sims); new Metal-specific Worker (clean fit to Metal's data model, can sync per-store with real merge semantics, and is itself a portfolio piece — while still stealing study-sync's proven patterns: Turnstile, token auth, baseUpdatedAt concurrency, offline-as-a-status). Tradeoff: duplicated auth plumbing across workers; accepted for isolation. Timing unchanged from BUILD_PLAN: the app must be fully usable without sync (offline-first is a hard constraint), so the Worker is designed in v1 documents but built at Stage E — building it earlier delays studying (RISKS.md R3). My question to Christopher: confirm the timing — new Worker at Stage E as recommended, or do you want sync in v1 badly enough to grow Stage B's scope? → Christopher confirmed 2026-07-19: new Worker, Stage E timing.
Status: ratified at Gate 0 (2026-07-19).

**D-010** | Stage 0 | _v1 curriculum scope: modules M1–M7 (runnable-heavy core); M8–M10 (GPU kernels, distributed training, compilers) authored as theory+simulator modules in Stage D._
Options considered: everything in v1 (curriculum authoring is the single biggest effort in this build — see RISKS.md R2 — and would delay actual studying by months); core-first (M1–M7 are prerequisite for M8–M10 anyway and are mostly runnable on the laptop, so studying starts at Stage A). Tradeoff: the flashiest topics (distributed training) come last; correct order pedagogically anyway. My question to Christopher: does the module list in docs/CURRICULUM.md match what you want to learn before Fall 2027?
Status: ratified at Gate 0 (2026-07-19).

**D-012** | Stage A | _Public GitHub repo named `bare-metal` (→ live URL mofchris.github.io/bare-metal/)._
Options considered: `metal` (short but generic — collides with Apple's Metal API in searches); `bare-metal` (matches the local folder, and "bare metal" is the systems term for running close to hardware — on-theme for MLSys). Public from day one: the repo is a portfolio piece and the history should show the whole build. Tradeoff: renaming later breaks the Pages URL and `base` path, so this wants to be settled now. My question to Christopher: veto within Stage A if you want a different name — cost of renaming grows once the URL is shared anywhere. → Confirmed by Christopher 2026-07-19: `bare-metal`.
Status: ratified 2026-07-19.

**D-013** | Stage A | _Formatter: Prettier (default config, printWidth 90), enforced in CI. Linter: deferred._
Options considered: Prettier alone (zero-decision formatting, covers the CLAUDE.md consistency rule; `format:check` gates the deploy); Prettier + ESLint (adds real static analysis, but its value overlaps heavily with TypeScript strict mode in a small codebase — revisit when the codebase or contributor count grows); nothing (style drift, rejected outright). Tradeoff: some bug classes ESLint would catch are left to strict TS + tests for now. My question to Christopher: none.
Status: ratified at Gate A (2026-07-19).

**D-014** | Stage A | _Content compiler dependencies: `yaml` (~parse authored YAML) and `marked` (Markdown → HTML at build time). Both dev-only._
Options considered: js-yaml (fine, but `yaml` has better TypeScript types and maintenance cadence); remark/unified for Markdown (powerful plugin pipeline we don't need yet); marked (small, boring, widely used, synchronous API). Per CLAUDE.md both are liabilities: justified because hand-rolling a YAML or Markdown parser is weeks of correctness bugs for zero learning value in scope. Neither ships to the client — they run only at build time, so the runtime bundle stays at ~5 KB. Tradeoff: marked's default HTML output is unsanitized; acceptable because all input is our own repo-reviewed content, not user input (re-evaluate if content ever comes from elsewhere). My question to Christopher: none.
Status: ratified at Gate A (2026-07-19).

**D-015** | Stage A | _Compiler runs via Node's native TypeScript type-stripping (`node cli.ts`); validation is hand-rolled, not a schema library._
Options considered: tsx/ts-node to execute TS tools (extra dependency doing what Node ≥ 22.18 now does natively — our Node 24 runs .ts directly); compiling tools with tsc first (build-step bookkeeping for no gain). Validation: zod (nice API, but a ~14 KB dependency to express ~15 checks) vs hand-rolled validators with error accumulation (plain conditionals a stranger can read; every problem reported in one pass, tagged with the offending file). Tradeoff: hand-rolled checks must be updated by hand as the schema grows — acceptable, the schema is ours and small. Constraint accepted: type-stripping requires `.ts` extensions on relative imports in tools/ (`allowImportingTsExtensions`). My question to Christopher: none.
Status: ratified at Gate A (2026-07-19).

**D-016** | Stage A | _Navigation uses hash routing (`#/lesson/<id>`), not the history API._
Options considered: history-API routes like `/lesson/m1/x` (cleaner URLs, but GitHub Pages is a static file server — refreshing or direct-linking such a URL 404s unless we add a 404.html redirect hack); hash routes (the fragment never reaches the server, so refresh, bookmarks, PWA launches, and offline all work with zero server config). Tradeoff: URLs are slightly uglier; irrelevant for a personal study app. Implementation is ~20 lines in `src/lib/route.ts` — no router dependency. My question to Christopher: none.
Status: ratified at Gate A (2026-07-19).

**D-018** | Stage B | _Spaced repetition algorithm: a binary-grade SM-2 variant, hand-implemented (~40 lines)._
Options considered: full SM-2 (expects 0–5 self-rated quality per review; our quizzes produce only right/wrong, so the extra machinery would run on invented inputs); FSRS (state of the art, measurably better retention modeling, but a complex model — as a first SRS it's an unexplainable black box, and dependencies are liabilities); Leitner boxes (dead simple, but no per-question difficulty adaptation — every question marches the same ladder); SM-2-binary (SM-2's skeleton driven by our real binary signal: correct → interval 1 d, 6 d, then ×ease (start 2.5); wrong → lapse, ease −0.2 floored at 1.3, interval back to 1 d; interval capped at 90 days so nothing vanishes past the MSc date). Tradeoff vs FSRS: some retention efficiency given up for an algorithm Christopher can fully explain — which is itself a Gate D-style learning goal. Revisit with real usage data at Stage E. My question to Christopher: none.
Status: pending (ratify at Gate B).

**D-019** | Stage B | _Restore merges rather than replaces; SRS state is never merged — it's rebuilt from the merged attempts._
Options considered: replace-on-restore (simple, but restoring a laptop backup on the phone would erase phone-only history — exactly the data loss the backup exists to prevent); merge (union). Merge rules: `attempts` union with identity = (questionId, at, sessionId) — append-only records are immutable, so equal keys are the same event, making restore idempotent; `lessonProgress` per lesson: "done" beats "in-progress", two "done"s keep the EARLIER completedAt (the historical first completion); `srsState` is not merged at all — it's derived state, so after merging attempts it is rebuilt by replay (one code path, no reconciliation bugs possible); `meta` stays local (installId identifies the device, lastExportAt is per-device). Tradeoff: replay cost on restore — linear in history, measured trivial (the 5000-record perf pin covers the same path). My question to Christopher: none.
Status: pending (ratify at Gate B).

**D-020** | Stage B | _Design pass pulled forward from Stage E to now, at Christopher's request._
Options considered: keep design in Stage E as planned (plan purity, but Christopher said plainly he doesn't _want_ to open the current app — and Gate B requires a week of daily use, so an app he avoids blocks the gate and feeds risk R3); do the pass now (one block of work, ~10 components + 1 stylesheet, no architecture changes, makes the daily-use gate easier to pass honestly). Constraint that shapes the design: offline-first is non-negotiable — no CDN fonts or external assets; anything visual ships in the repo and rides the service-worker precache. Tradeoff: a day not spent on M6/M7 authoring. My question to Christopher: none — this was his call, made 2026-07-19.
Status: ratified 2026-07-19 (Christopher's explicit request).

**D-021** | Stage B | _Home is dashboard-first: a data-driven hero pushing exactly one action, the module map, and a progress side rail; the Progress page stays as the detail view._
Options considered: keep home = module list and dashboard separate (extra click between "open app" and "know what to do"); merge everything onto one page (the full mastery list makes home unscannable); dashboard-first home with one pushed action (start → review → continue, chosen from data) plus overview panels, detail kept at #/dashboard. Empty state teaches the loop in the app's own via language and pushes lesson 1. Full-width desktop layout (84rem shell, main + 19rem rail grid); reading surfaces stay a 44rem column. Requested and directed by Christopher 2026-07-19 ("I actually want to use the app"). My question to Christopher: none.
Status: ratified 2026-07-19 (his direction).

**D-023** | Stage B | _Progression gating + module exams: 75% to advance (Christopher's direction)._
Options considered: free browsing (how it was — no stakes, but nothing pushes mastery before moving on); hard gating (his request): within a module, lesson N+1 opens at ≥75% on lesson N's quiz; a module's exam (all of its questions, one run) opens when every lesson is passed; a module opens when ALL its prereq modules' exams are passed (uses the existing prereq graph, so M5 needs both M3 and M4). Data: lessonProgress gains bestScorePct (best across retakes, first completedAt kept); new examResults store (DB v3, backup v3, merge best-wins). Legacy score-less "done" records grandfather as passed. Direct URLs to locked content hit a guard page saying exactly what unlocks it; with storage unavailable, gating can't be evaluated and content stays open. Tradeoff: gating can frustrate (a 74% blocks progress) — mitigated by unlimited retakes and the review deck staying always available. My question to Christopher: none — his call, 2026-07-19.
Status: ratified 2026-07-19 (his direction).

**D-024** | Stage B | _Study-time tracking + 30-minute streak (Christopher's gamification request)._
Options considered: keep attempt-based streaks (no time signal — reading lessons counted for nothing); precise per-second session tracking (visibility/focus bookkeeping complexity for false precision); a 15-second heartbeat (while a lesson/quiz/review/exam screen is open AND the tab visible, credit 15 s to today) — honest to ±one tick per sitting, idle-tab-proof via the visibility check. Data: one row per (day, installId), seconds only grow → cross-device merge is max-per-row, sum-per-day, idempotent (DB v4, backup v4). Streak rule: a day counts at ≥30 tracked minutes; days from before tracking existed (attempts but zero time rows) grandfather in. UI: month calendar with goal-met/partial/none/today states, live today-minutes bar, streak count — replaces the 8-week strip on home and Progress. Also: the hero's radial gradient REMOVED (visible banding on Christopher's LCD — dark radial gradients band on 8-bit panels) and replaced with a flat-stroke animated PCB-trace SVG, which cannot band. My question to Christopher: none — his direction, 2026-07-19.
Status: ratified 2026-07-19 (his direction).

**D-022** | Stage B | _Cross-device sync via the EXISTING study-sync Worker — supersedes D-011 (which chose a separate Worker), at Christopher's direction and on new facts._
Options considered: build the Metal-specific Worker D-011 planned (clean, but duplicates auth/rate-limiting/D1 plumbing that already exists and is battle-tested by two apps); reuse study-sync (D-011's objection was that Metal's multi-store IndexedDB "doesn't fit a single-blob PUT" — that objection died when D-019 built the backup file: a lossless single-JSON snapshot with idempotent client-side merge, which IS a blob). Worker changes (tested, deployed 2026-07-19): `app=metal` allowed; blob cap 256 KB → 1 MB for Metal's append-only history. Design: sync = GET blob → merge into local via restoreBackup (D-019/D-024 rules) → PUT merged snapshot with baseUpdatedAt compare-and-swap → on 409, merge the returned blob and retry once. PUT is skipped when stores are byte-identical to the server's (respects the Worker's D1 write budget). Auth: username + 6-digit PIN, token under the same localStorage key as GRE/Net+ — same origin, so ONE account spans all three study apps (feature, not accident). Offline-first exception, recorded: Cloudflare Turnstile's script loads from its CDN on the auth form only — sign-in inherently needs network; studying never does. Tradeoff accepted: ~90-day sessions require re-login; sync failures surface in the Backup page, never block studying. My question to Christopher: none — his direction 2026-07-19.
Status: ratified 2026-07-19 (his direction).

**D-017** | Stage A | _Mid-quiz durability via per-answer attempt writes, not an autosaved quiz snapshot; tests run on `fake-indexeddb` (new dev dependency)._
Options considered: GRE-sim-style `inprogress` snapshot with resume (serializes whole quiz state every ~10 s; needs resume/discard UI and a snapshot format to maintain); write each attempt to the append-only store the moment it's graded (killing the app loses at most the question on screen, no snapshot format, no resume UI — a 5-question quiz doesn't need resumption). The append-only store was already the DATA_MODEL ground truth, so the snapshot would have been redundant machinery. Tradeoff: no mid-quiz resume — you restart the quiz, but answered questions are already in history. Test infra: `fake-indexeddb` (dev-only) polyfills IndexedDB in Node so the Gate A persistence tests are automated rather than manual-only; alternative was browser-based test running (heavier, slower, not needed yet). My question to Christopher: none.
Status: ratified at Gate A (2026-07-19).

**D-025** | Stage B | _Content teaching convention: "no naked terms" — every term is grounded in flow at first use, and a gloss may not be built from other abstractions (Christopher's direction, from a screenshot of m1/02)._
Options considered: a prerequisites/basics module up front (rejected by Christopher — "not a module that will just slap the basics on there in the beginning"); a tappable glossary UI with term chips (real cost: compiler change, new component, glossary store — and it makes him tap mid-sentence on a phone); inline prose glosses (chosen): one clause to one sentence, same paragraph, before or immediately after the term does any work. THE FLOOR: a CS degree — bits/bytes, binary, variables/arrays/loops/functions, compiler vs interpreter, RAM, basic calculus notation are assumed; everything hardware-specific and everything ML-specific gets grounded. THE SHARPENED RULE, learned by breaking it: a gloss may not contain a term that itself needs a gloss. The first draft of m1/02 defined "instruction-level parallelism" as "parallelism the chip extracts from one ordinary single-threaded program" — circular, and leaning on "thread," which the lesson never introduced. Christopher had to leave the app and Google it. Abstract nouns are the tell (parallelism, execution, concurrency, extraction, utilization): if the gloss leans on one, it has not landed. Also required: units say what they count on first appearance and get anchored to something already real (3 GHz = 3 billion ticks/s = one cycle every 0.33 ns, so lesson 01's 100 ns DRAM trip is ~300 wasted cycles); forward references get a one-line placeholder gloss at the point of USE, not just a promise that M5 will explain it. Enforcement: content/GLOSSARY.md is the authoring ledger (term → gloss → the lesson that first grounds it), consulted before authoring any new lesson. A compiler check that fails the build when a term appears before its declared first-grounding lesson was considered and DEFERRED — cheap to add, but it earns its keep when M6–M10 are authored, not now. ACCEPTANCE TEST, and it is Christopher's, not mine: if a sentence sends him to Google, that sentence is a bug. I am the wrong instrument to detect this failure — I know what the words mean, so the gap is invisible from where I stand; he caught in thirty seconds what my own audit missed entirely. Tradeoff: lessons grow 20-95% in length (m1/02, the worst offender, went 76 → 166 lines). Facts, numbers, sources, and objectives are never changed by this pass — it adds grounding only. My question to Christopher: none — his direction, 2026-07-21.
Status: ratified 2026-07-21 (his direction).

**D-026** | Stage B | _Prose contract for all lesson content: 22 testable rules restoring causal connective tissue — supersedes D-025's approach while keeping its term ledger (Christopher's direction, after an independent writing review)._
Options considered: copy-edit the existing prose (rejected — the defect is DELETION, and you cannot edit a sentence that was never written); rewrite from scratch (rejected, though Christopher offered it — it would discard genuinely good assets: module and lesson sequencing, the frontmatter objectives, a real sourced reading list, the tables, and D-025's term inventory); paragraph-level rewrite against a fixed contract (chosen) — id, title, objectives, sources, section sequence, all numbers and all tables held constant, with the argument between sentences restored. THE DIAGNOSIS: the prose was compressed, not vague. Facts correct and well ordered; mechanism, derivation, antecedent and finite verb deleted for rhythm. Christopher: "i dont see how any sentence in that paragraph connects to any other one... it reads more like a robot that expects me to know whats missing." D-025 MADE ONE AXIS WORSE: "no naked terms" was satisfied the cheapest way that passes, by injecting glosses as em-dash asides inside existing sentences — m1/02 reached 39 em-dashes in 1473 words and its peak-FLOPS payoff sentence had no main verb. THE 22 RULES, each with a check: R1 mechanism before consequence; R2 no unstated intermediate steps; R3 every number derived in the paragraph it appears in; R4 problem stated before fix; R5 every simplification reconciled where it collides with a real number; R6 one idea per sentence, 25 words max; R7 at most one em-dash pair per sentence, never between subject and verb; R8 a definition gets its own sentence with the term as grammatical subject; R9 no sentence fragments; R10 pronoun refers to the most recent candidate noun; R11 no "the X" at first mention; R12 closers restate rather than perform; R13 headings state the question the section answers; R14 paragraphs max 5 sentences and ~80 words (iPhone reading); R15 comprehension check in every lesson; R16 analogy after the mechanism and deletable; R17 banned assumed-membership register (famously, cardinal sin, wearing X's clothes, the receipts); R18 no unmarked allusions; R19 one meaning per term across the curriculum; R20 ground component nouns before bolding a compound; R21 no promise the repo cannot keep; R22 frontmatter is an input, never edited. LABS: ten labs (L1.1 to L5.2) were cited in body prose as existing; none do, and neither do modules M6-M10. Christopher's call: cut all references entirely rather than re-tense them. ALSO FIXED, found by the review: m4/04's synthetic-data arithmetic was ambiguous-to-wrong (now explicit times), and m1/02's five-stage pipeline contradicted its own 15-20 cycle misprediction cost (now reconciled). RESULT: 1,493 to 3,016 lines; em-dashes 120+ to 7; comprehension checks 1 to 21; term audit clean. ACCEPTANCE TEST unchanged from D-025 and still Christopher's: if a sentence sends him to Google, that sentence is a bug. My question to Christopher: none — his direction, 2026-07-22.
Status: ratified 2026-07-22 (his direction).

**D-027** | Stage B | _Curriculum restructured to the Fall 2027 roadmap's MLSys study syllabus: six new modules added, two demoted to optional, order by dependency — answers the question D-010 left open (Christopher's direction)._
Context: D-010 scoped v1 as M1-M7 with M8-M10 as Stage D, and its own open question was "does the module list in docs/CURRICULUM.md match what you want to learn before Fall 2027?" The roadmap PDF (Christopher_Fall_2027_Admissions_Funding_Study_Roadmap, dated 21 July 2026) answers it on page 7, "MLSys study syllabus", which lists 11 modules in two columns: STUDY and OUTPUT / PROVING MASTERY. THE KEY DISTINCTION, Christopher's own framing: the two flagship repositories (cheapserve-mlsys, trace-to-speed) are competence evidence; Metal is the general MLSys knowledge base. So the STUDY column is Metal's contract and the OUTPUT column belongs to the repositories. Metal is not a flagship and does not compete with them. CROSS-REFERENCE FINDING: of the 11 roadmap modules, only 2 were substantially covered (benchmark discipline by M2, computer architecture by M1), 4 partially (quantisation concepts in M3, serving metrics in M2/04, generic profiling in M2/03, PyTorch fragments across M4 and M5), and 5 had no home at all (Linux + OS, ONNX Runtime, C++/CUDA foundations, research communication, distributed). Worse, the disagreement ran both ways: Metal planned two modules the roadmap does not ask for (GPU architecture and kernels, ML compilers) while six things it does ask for were absent. ADDED, numbered in dependency order so the app's prereq gating matches the learning order: M6 Linux and the OS underneath (prereq M1); M7 Research communication (prereq M2 — placed early because it needs only measurement discipline and improves every write-up after it); M8 The PyTorch execution model (prereq M5 — inference_mode needs the tape, strides need cache lines); M9 Model compression (prereq M3, M5); M10 Runtimes and export, ONNX Runtime (prereq M8 — you export from PyTorch); M11 Profiling ML workloads (prereq M8 — kept separate from M2/03, which is generic Python profiling needing only M1); M12 Inference serving (prereq M6, M9 — queues and backpressure are OS ideas); M13 C++ foundations (prereq M8, CUDA last per the roadmap's "only after CPU work is correct"); M14 Distributed basics (prereq M5, sized to the roadmap's "toy experiment or design note"). DEMOTED to optional MX1/MX2: GPU architecture and kernels, ML compilers — absent from the syllabus, and partly covered by M10's graph-optimisation lesson and M13's CUDA lesson. PATCHES to built modules: NUMA into M1; confidence intervals, hardware/software disclosure and raw-result storage into M2. SIZING: 42 new lessons on top of 21 built, roughly 3 months at the roadmap's 8 focused study hours per week. That 8 hours is STUDY ONLY, confirmed by Christopher — building the flagship repositories comes from separate hours, and my build time is not drawn from it either. ORDER: by dependency rather than by flagship deadline, also his call — Metal is a knowledge base, so coherence beats schedule. Tradeoff accepted: sequencing by dependency means a topic is occasionally learned after the repository work that would have used it. LEFT OPEN, deliberately: whether to build labs at all. D-001 and D-010 called the core "runnable-heavy", but the roadmap assigns the artifact burden to the repositories, so a Metal lab would now exist only to make a concept stick. No labs exist today; this is recorded as open rather than quietly dropped. My question to Christopher: labs, yes or no?
Status: ratified 2026-07-22 (his direction).

**D-028** | Stage B | _Checkpoint quizzes: after every two modules, an OPTIONAL mixed quiz drawn from those two modules' banks — never gates progression (Christopher's request)._
Options considered: author new cross-module synthesis questions per checkpoint (highest pedagogical value, but ~70 new questions across 7 pairs, and it competes with authoring the remaining modules); draw a sampled subset from the two modules' existing banks (chosen — honestly "covering the previous two modules", zero fabrication risk, auto-syncs as questions are edited, and matches the app's existing spaced-review philosophy of re-encountering questions). Design: `src/lib/checkpoints.ts` pairs consecutive modules in curriculum order (1&2, 3&4, …), so pairs extend automatically as M9-M14 are authored; a trailing odd module has no checkpoint until its partner exists. `checkpointQuestions` draws CHECKPOINT_SIZE (12) questions balanced across the two banks, interleaved so the modules alternate, via an injected rng (Math.random in the app for a fresh mix per visit, seeded in tests). OPTIONAL means UNGATED: the checkpoint reuses the Quiz component with no examModuleId and no markDoneLessonId, so it records attempts and feeds the SRS schedule like any quiz, but sets no lesson or exam status and is never a prerequisite for anything (contrast D-023's gated lesson quizzes and module exams). Route `#/checkpoint/<firstModuleId>` (route.ts), rendered by an ungated CheckpointScreen in app.tsx that samples once per visit via useMemo keyed on the checkpoint id (so a study-time tick or an answer does not reshuffle mid-quiz). Home shows a copper-tinted CheckpointRow after the second module of each pair, active once both its modules are unlocked and muted before that ("opens once you reach <B>") — visibility only, never a hard lock. Tooling: audit-terms.mjs already derives module order from module.yaml, so nothing there needed changing; new tests in checkpoints.test.ts (8) and route.test.ts (checkpoint parse), 81 total green. My question to Christopher: none — his request, 2026-07-23.
Status: ratified 2026-07-23 (his request).

**D-029** | Stage B | _Daily review quiz on first open of the day, with failed questions routed to remediation (Christopher's request, 2026-07-25) — PENDING._
His request: a random questionnaire drawn from units and modules he has already completed, suggested on the first login of every day, staying on screen until he either dismisses it or attempts it; any question he fails should then "more or less crash course explain that stuff." OPTIONS for the crash-course half: (a) author a dedicated remediation corpus per question — most authoring, and it duplicates prose the lesson already contains, so it also carries the highest fabrication risk of the three; (b) re-show the D-026 explanation, which already derives the answer with arithmetic — free, but he has just read it inside the quiz, so it teaches nothing new on the second showing; (c) deep-link to the exact lesson SECTION that grounds the question, so the remediation IS the authored lesson prose — costs a new `section` field per question plus a compiler check that the anchor resolves, and zero new content. RECOMMENDED: (c) as the primary path with (b) inline above it. Rationale: the D-025/D-026 passes exist precisely so that the lesson explains things properly in flow; a parallel remediation corpus would immediately drift from it and would have to be maintained twice. OPEN DESIGN QUESTIONS: what counts as "already completed" (D-023 lesson status, or module-exam-passed, or either); quiz size; whether attempts feed the SRS schedule and the streak (they should, matching D-028's ungated-but-recorded model); and how "dismissed today" persists — that needs a new store and therefore a DB version bump, which after the v3 blocked-upgrade hotfix means blocked/blocking handlers are mandatory, not optional. Sequencing note for Christopher: he asked for this AFTER M11–M14, but Gate B's own manual test is "a week of real daily use logged here before sign-off," and this feature plus D-031 is the habit machinery that produces that week — there is a real argument for pulling it earlier. His call.
Status: ratified 2026-07-25 (delegated: "do what you determine is right"). CHOSEN: option (c) — a failed question deep-links to the lesson section that grounds it, with the D-026 explanation shown inline above the link. Sequencing resolved my way and against his earlier "for after": D-029/D-030 land BEFORE M11–M14, because the repeated-questions friction is in his daily study loop right now and every week of authoring that follows compounds on top of a fixed loop.

**D-030** | Stage B | _Question-bank variety: retries currently serve the identical questions in the identical order (Christopher's report, 2026-07-25) — PENDING, and his stated targets are not all reachable._
The bug is real and confirmed in code, not just felt: `questionsFor()` in `src/lib/lookup.ts` returns every question attached to a lesson in bank order, and the quiz screen serves that list whole. A retry after a failed attempt is therefore the same five questions in the same sequence — he is re-testing recall of a specific screen, not of the idea. HIS TARGETS: per lesson quiz, 20 takes with no repeated question, and 6 takes with no repeat even rephrased; for the daily quiz, no repeats before 10 takes; failed questions must still appear in review/weakest-areas and must still count toward metrics however scoring is defined. THE ARITHMETIC, because it decides the design: a lesson quiz serves 5 questions, so 20 clean takes needs 100 distinct items per lesson, which across 43 lessons is roughly 4,300 items against the 217 that exist — a 20x bank. The 6-take bar is the harder one and it is not an effort problem: 6 takes with no rephrasing means 30 conceptually DISTINCT questions per lesson, while a lesson teaches about five ideas. You cannot ask thirty genuinely different things about material that contains five without asking about things the lesson never taught, which fails D-025's floor and the no-fabrication rule at the same time. OPTIONS: (a) hand-author toward the stated numbers — months of authoring, competes directly with M11–M14, and hits the impossibility above regardless; (b) parameterised question templates for the computational tier, where one authored template plus a parameter table expands into many real items and the compiler DERIVES each answer and explanation from the parameters rather than inventing them (e.g. DRAM latency in {80, 100, 120, 150} ns against a clock in {2.4, 3.0, 3.6} GHz gives twelve honest variants of the cycles-wasted question, with distractors that encode the same misconceptions in every variant) — this is honest generation because the arithmetic is computed, not asserted; (c) keep the bank as is and simply shuffle, which fixes order but not repetition. RECOMMENDED: (b) plus per-item seen-history, targeting roughly 30 items per lesson — about 6 takes before any item repeats — and, past that point, preferentially re-serving items he previously got WRONG rather than chasing novelty, since re-encountering a specific misunderstanding is the pedagogically valuable repeat and is what the SRS engine already believes. The 10-takes-clean target for the daily quiz IS reachable today: it draws across all completed modules, so the pool is already 217. TRADEOFF TO ACCEPT EXPLICITLY: his stated 20x/6x numbers get traded for roughly 6x plus targeted repetition of his own errors.
Status: ratified 2026-07-25 (delegated). CHOSEN: option (b), parameterised templates, plus a seen-aware selection engine. IMPLEMENTATION FINDING that removes the scariest part of this work: seen-history needs NO new store and NO schema version bump, because `attempts` is already append-only ground truth carrying questionId, correctness and an ISO timestamp for every answer ever graded. "Which questions has he seen, when, and did he get them right" is therefore DERIVED, exactly like srsState — which means no v5 migration and none of the blocked-upgrade risk that produced the 2026-07-19 hotfix. ORDER OF WORK, because it decides what he actually sees: the selection engine alone changes nothing for a lesson quiz whose pool is 5 and whose quiz size is 5 — the quiz IS the bank, so there is nothing to rotate. Selection lands first anyway (it is pure, testable, and immediately improves checkpoints and the D-029 daily quiz, which both draw from the full 217), then template expansion in the content compiler grows the per-lesson pools, at which point lesson-quiz rotation switches on by itself. HIS TARGETS ARE EXPLICITLY NOT MET and he has been told so in plain terms: roughly 6 clean takes, not 20, and no honest path to 6 rephrasing-free takes at all.

**D-031** | Stage B | _A quote on every app open, one per day of the year, anime-sourced and attributed (Christopher's request, 2026-07-25) — PENDING, with an authenticity problem to solve first._
His request: 365 quotes, one per day, each carrying the name of the person who said it; roughly 35% from Blue Lock, then Demon Slayer and Solo Levelling, with Kaiju No. 8 and Attack on Titan as respectable mentions; explicitly "it doesn't always have to be warm and rosy." THE PROBLEM: 365 attributed lines written from my own recall would produce confident misattributions and, worse, lines that were never said — the failure mode CLAUDE.md rules out absolutely, and one Christopher would personally catch, since he is the person in the room who has actually watched these shows. It is also genuinely hard to get right: the English wording of any given line differs across the official manga translation, the Crunchyroll subtitles and the dub, so "the quote" is not one fixed string, and 35% of 365 is 128 distinct memorable Blue Lock lines, which is far past the well-known ones. OPTIONS: (a) write them from memory — rejected outright, this is fabrication with a character's name attached; (b) source each against the web and record where it came from — honest, slower, a one-time cost, and it makes the file auditable later; (c) ship a smaller pool that is fully verified, drive it with day-of-year modulo pool size so the app never depends on the number 365, and let him add lines himself in a YAML file whenever one lands. RECOMMENDED: (b) to build a first pool of roughly 100–150 with a recorded source per line, and (c) as the permanent growth mechanism and the app-side design. Decoupling from 365 is the load-bearing part: it means the feature ships at whatever pool size is honestly verified instead of waiting on a number, and it degrades gracefully rather than repeating within a year in an obvious way. Also worth his ruling: whether repeats within a year are acceptable at all, and whether he wants the "not warm and rosy" register weighted (Ego Jinpachi and Barou land very differently from Tanjiro).
Status: ratified 2026-07-25 (delegated, with one explicit instruction he added: "for the quotes i want some of egos isagi and nagis original quotes"). CHOSEN: (b) source-verified seed pool + (c) day-of-year modulo pool size with a YAML file he extends himself. His addition sharpens the Blue Lock third — Ego Jinpachi, Isagi Yoichi and Nagi Seishiro are named must-haves, and "original quotes" is read strictly as the actual lines rather than paraphrases, which is the whole reason for the sourcing pass. That trio also spans the register he asked for on its own: Ego is contemptuous, Isagi is hungry, Nagi is flat and unbothered.

**D-032** | Stage B | _A "what changed" note shown in-app on first open after an update, not only in the terminal (Christopher's request, 2026-07-25) — PENDING._
Said in passing while describing D-030 — "just let me know what's changed here in the terminal and in app on first open after the update" — and recorded separately so it is not lost inside a question-bank decision, because it is a distinct feature with its own failure mode. The terminal half already happens by convention every session. The in-app half does not exist. It matters more than it sounds: Metal is an installed PWA that updates silently in the background, so behaviour can change under him between one open and the next with nothing on screen to say so — exactly the confusion that produced the "why are the questions the same" report in the first place. Sketch: a build-stamped notes file compiled alongside the curriculum, shown once per version on first open, dismissible, never blocking. OPEN: whether it is a banner or a full screen, whether old entries stay readable afterwards, and whether it appears for content-only changes or only for behaviour changes.
Status: ratified 2026-07-25 (delegated). CHOSEN: a build-stamped notes file compiled alongside the curriculum, shown once per version on first open, dismissible, never blocking; behaviour changes only, since a content-only edit would fire it constantly during authoring and train him to dismiss it unread.

**D-033** | Stage B | _No labs in Metal; BUILD_PLAN Stage C amended rather than left to rot (Christopher's delegation, closing the question D-027 left open)._
D-027 deliberately recorded "labs, yes or no?" as open rather than quietly dropping it, and it stayed unanswered across three sessions while blocking M11 — the most lab-shaped module left, where a lab retrofitted afterwards costs more than one authored in place. DECIDED: no labs. Reasoning: the Fall 2027 roadmap assigns the artifact burden to the two flagship repositories (cheapserve-mlsys, trace-to-speed), and D-027 already settled that Metal is the general knowledge base rather than competence evidence, so a Metal lab would exist only to make a concept stick. Against that thin benefit sits an entire BUILD_PLAN Stage C — a harness that runs his code, measures it, verifies targets and feeds results back into the web app — which would compete directly with finishing a curriculum that is 10 modules of 14 done. CONSEQUENCE, and the reason this is a decision rather than a preference: the ten labs L1.1–L5.2 were already deleted from the curriculum in the 2026-07-22 rewrite because they were referenced as if they existed while no files backed them, so "no labs" is now the honest description of the repo rather than a change to it. M11 teaches operator-level and memory profiling and points at cheapserve as the thing to profile, which puts the measurement where the roadmap wants it. BUILD_PLAN Stage C is amended in place per standing rule 2 (propose an edit, never quietly deviate) rather than deleted, so the reversal stays legible to anyone reading the plan later. Reversible: if he later wants a lab, the stage text and this entry say exactly what was given up.
Status: ratified 2026-07-25 (delegated).

**D-034** | Stage B | _Worked practice problems inside lessons: graded difficulty, revealable hints, answer hidden until asked for (Christopher's report, 2026-07-26) — PENDING._
WHAT HE HIT: m1/03 says "Work three examples by hand, because doing this yourself is the skill" and then immediately works all three for him. The sentence is simply false — there is nothing there to work. He asked for real problems, difficulty rising with each one, and optional hints he can reveal when stuck. THE AUDIT MAKES IT BIGGER THAN ONE LINE. Every lesson does carry a "Check your understanding" section, so the intent was always there, but 34 of the 43 print "A correct answer says..." in the same paragraph as the question. There is nowhere in Metal where he can attempt something before being told the answer — the eye reaches the solution before the brain reaches the problem. The m1/03 line is the only place that also PROMISES examples it does not supply, which is why he caught it there first, but the reveal problem is curriculum-wide. OPTIONS: (a) raw HTML <details> blocks in the Markdown — works today with zero code, but it is unstyled, gives no way to tier hints, and puts presentation markup into content files the compiler cannot validate; (b) a compiler-recognised practice block — structured entries carrying `problem`, an ordered `hints` list and `answer`, validated like everything else and rendered by the app with staged reveal (first hint, next hint, then the answer), so difficulty order is explicit and a lesson promising three problems can be REQUIRED by the build to contain three; (c) push practice into the question bank instead — rejected, because bank questions are graded and gate progression (D-023), whereas practice must be safe to fail. RECOMMENDED: (b). It also composes with D-030: the template machinery already computes numbers from formulas, so a practice problem can be parameterised the same way and graded difficulty becomes a matter of ordering the parameter sets rather than hand-writing three separate problems per lesson. SIZING, honestly: the mechanism is perhaps a session; the content is 43 lessons times roughly three problems, which is the larger half and should follow the same "one module ahead of where he is studying" rule the templates now use rather than a single sweep. INTERIM QUESTION FOR HIM: the m1/03 sentence is false TODAY, in a lesson he is reading — reword it now as a one-line honesty patch, or leave it until the real fix lands so the lesson is only touched once?
Status: ratified 2026-07-26 (delegated: "handle all except the quote stuff"). CHOSEN: option (b), a compiler-recognised practice block placed by a {{practice:N}} marker, rendered as native <details> so hints and answers hide with no JavaScript. The build now REJECTS a problem that is never placed, a marker with no problem, and a list whose difficulty falls — he asked for rising difficulty, so it is enforced rather than trusted. m1/03's false "work three examples" sentence is fixed and now carries the three problems it promised, and the new m1/01b carries three more. MECHANISM DONE, CONTENT ONGOING: 42 lessons still print "A correct answer says..." in the same paragraph as their question. Those get problems one module ahead of his reading, the same rule the D-030 templates follow, rather than in one sweep.

**D-035** | Stage B | _Lesson reading progress: a scroll-position bar at the top of a lesson, and a resume-where-you-stopped bookmark (Christopher's request, 2026-07-26)._
His request, from using the GRE study units: a bar across the top showing how far through a lesson he has scrolled and how much is left, plus a way to return to where he stopped last session "instead of just having to start scrolling from the top of the page". The second half is the load-bearing one — lessons run 120 to 150 lines of rendered prose and he reads them across several sittings on a phone, so every resumed session currently costs a hunt for the paragraph he left off at. WHY THIS IS CHEAP: reading position is a number per lesson, and the `meta` store already holds arbitrary key/value strings, so this needs no new object store and no schema version bump — the same reason D-029 was cheap. DESIGN: the bar is a fixed-position element driven by scrollY over (scrollHeight - innerHeight), updated on scroll; position is saved throttled rather than on every scroll event, because an IndexedDB write per scroll tick would be absurd. Restore offers rather than forces — jumping the reader somewhere unexpected on load is disorienting, and a lesson he actually finished should not fling him to the bottom, so a fraction at or past the end is treated as "finished" and restores to the top. Stored as a FRACTION rather than a pixel offset: pixel offsets are meaningless across a phone and a laptop, and the same lesson reflows to a different height on each, whereas a fraction survives both (and survives content edits approximately, which is the best available). My question to Christopher: none — his direction.
CROSS-CHECK AGAINST THE GRE APP, done 2026-07-26 after he pointed out I had built this without looking at the implementation he was describing: GRE/js/course.js computes `max = scrollHeight - clientHeight` then `min(100, 100 * scrollTop / max)`, falling back to 100 when there is nothing to scroll. Metal's scrollFraction arrived at the identical formula independently, including that same fits-on-screen edge case, and additionally clamps the negative side for iOS rubber-band scrolling. Two differences, both correct for their app: GRE scrolls an inner container while Metal scrolls the window, and GRE has NO bookmark — it draws the bar and nothing more — so the resume-where-you-stopped half, which is the part he actually asked for, had nothing to copy from.
Status: ratified 2026-07-26 (his direction).

**D-036** | Stage B | _Christopher's DRAM-internals write-up: accepted as a source to work from, NOT as text to paste in — PENDING placement._
He supplied ten paragraphs covering why bandwidth scales and latency does not, Apple's on-package Unified Memory, DRAM internals (bank/row/column decoding, wordlines, sense amplifiers, the row buffer), the timing parameters tRCD, CL, tRAS and tRP, the memory controller's scheduling role, row-buffer hits versus misses, and the latency-hiding techniques CPUs use instead. ASSESSMENT, honestly: the technical content is sound as far as I can check it, and it genuinely answers a question m1/03 currently raises and then drops — the lesson asserts that "latency cannot be widened away" and explains it only by analogy (adding a lane does not shorten the road), where his material explains the actual mechanism (the charge in a DRAM cell is tiny and sensing it is an analog process that does not parallelise). That is a real gap and this closes it. TWO REASONS IT CANNOT SIMPLY BE PASTED IN. First, sources: every lesson's `sources` field is enforced by the compiler and exists because of RISKS.md R4, so these claims need attributing to real references before they ship, not just reading correct. Second, and more important, DENSITY: D-025 and D-026 exist because he told me the lessons threw terms at him and expected him to already know them. This material introduces bank, rank, bank group, wordline, sense amplifier, row buffer, precharge, four timing parameters, LPDDR, HBM, SoC, prefetcher and simultaneous multithreading. Dropped in as written it would regress the exact failure those two decisions fixed, and it would be my fault, not his — he supplied research, not lesson prose. OPTIONS FOR PLACEMENT: (a) fold the mechanism into m1/01 where DRAM latency is first quoted — keeps it where the question arises, but m1/01 is already the introductory lesson and this would double its weight; (b) a new M1 lesson sitting after 01, owning DRAM internals properly at the "no naked terms" standard, which is where the timing parameters and the row-buffer hit/miss distinction genuinely belong; (c) push it to M6, next to virtual memory and the TLB — wrong, because M6 is about the operating system and this is hardware. RECOMMENDED: (b), with a short mechanism sentence added to m1/01 pointing forward to it. My question to Christopher: (b) as a new lesson, or (a) folded into what exists?
Status: ratified 2026-07-26 (delegated: "handle all except the quote stuff"). CHOSEN: (b) — new lesson `m1/01b-dram-internals`, "Inside DRAM — why the first byte is slow", placed second in M1. THE ID IS DELIBERATELY 01b RATHER THAN A RENUMBER: lesson ids are keys in his stored `lessonProgress`, so renaming 02 to 03 and so on to make room would orphan every record he has. Order comes from module.yaml's list, not from the ids, so an out-of-sequence id costs nothing and protects his history. ONE CLAIM OF HIS I COULD NOT SOURCE AND THEREFORE DID NOT MAKE: his paragraph 2 says on-package memory "improves latency somewhat"; searching found strong support for the bandwidth half and no citable measurement for a specific latency figure. Rather than assert an unverifiable number, the lesson DERIVES the point instead — a signal covers about 15 cm/ns on a board trace, so a chip 3 cm away is a 0.4 ns round trip against a ~41 ns access, about 1%. That is stronger than the original claim, needs no source beyond physics, and makes his argument rigorously: on-package memory is a bandwidth technique that happens to shave a sliver of latency. The four timing parameters are worked into a derivation (22 ticks x 0.625 ns = 13.75 ns for a row-buffer hit; tRP + tRCD + CL = 41.25 ns for a miss; exactly 3x), which also gives the lesson its payoff — the row-buffer hit/miss distinction is a SECOND, independent reason sequential beats random, sitting underneath m1/01's cache-line argument. Three practice problems (D-034) walk that arithmetic, and five questions were added. TERM-ORDERING FALLOUT, caught by the audit and not by me: inserting a lesson before m1/02 and m1/03 meant it used `clock` and `bandwidth` ahead of the lessons that owned them. Both are now grounded in place in 01b and the ledger ownership moved, and a stale known-accepted row for `bandwidth` in m1/02 was deleted because that forward reference no longer exists. Audit CLEAN.

**D-037** | Stage B | _Streak-break quotes: a harsher line when he drops the chain, with the character's words kept verbatim and the study-context framing written in the app's own voice (Christopher's request, 2026-07-26) — PENDING one policy confirmation._
His request: when he kills his streak the quote should call him out — either point him at how to try harder, or rile him up by telling him he is useless or does not want it enough, with Ego from Blue Lock named as the register that lands on him. He restated the hard constraint himself: "don't make up anything they didn't actually say", while allowing "maybe [you] can just rephrase some stuff so it goes with the context of the platform and use case." THE TENSION IN THAT ALLOWANCE, which is why this is pending: rephrasing a real line so it fits a study app means the finished sentence is partly mine while the name underneath it is still the character's — which is the fabrication he ruled out one clause earlier, arrived at by a gentler route. RECOMMENDED POLICY, which gets him what he wants without that: keep the quoted line EXACTLY as said and attributed, and write the study-context framing as separate text in the app's voice, visibly not part of the quote. "You broke a four-day chain." in Metal's voice, then Ego's actual words underneath, does the job the rephrase was meant to do and leaves nothing invented in anyone's mouth. DESIGN: `content/quotes.yaml` gains an optional `tone` field (default `drive`, plus `harsh`), the home screen picks from the harsh set on a day the streak has just broken and from the rest otherwise, and study-time.ts already computes the streak so the trigger exists. Selection cannot stay purely day-of-year for the harsh case, since a break happens when it happens; the harsh line is chosen by day-of-year within the harsh subset, so it is still stable within a day. CONTENT NOTE: the harsh set needs Ego lines specifically, which means sourcing them the same way as D-031 — the one Ego line already in the file was verified, and the same care applies to the rest. My question to Christopher: confirm verbatim-quote-plus-app-voice-framing, rather than rephrasing lines into the study context.
Status: pending — awaiting Christopher on the rephrasing policy.

**D-038** | Stage B | _Offer study-sync sign-in on first open, rather than waiting for the user to find the Sync page (Christopher's request, 2026-07-26)._
His observation: one study-sync account already spans three apps (Metal, the GRE sim, the Network+ sim), so somebody who has used one or two of them already HAS an account when they first open the third — and today Metal says nothing about it. They have to discover the Sync page on their own to find out their progress could follow them. He asked for a first-open prompt offering to sign in with the existing account. DESIGN: on first open with no auth token and no local history, Home offers a single quiet prompt — sign in to bring progress across, or dismiss — with dismissal remembered in `meta` exactly as D-029's card does, so it never nags twice. HONEST LIMIT worth recording: the app CANNOT detect whether he has used the other two apps. They are separate origins with separate localStorage, so there is no way to check from inside Metal short of asking the Worker, and asking the Worker requires the credentials the prompt is trying to obtain. So the prompt is offered to every new install rather than only to users known to have an account, and its wording has to work for both — "already using the GRE or Network+ sim? sign in and your progress follows you" reads correctly whether or not they are. Getting that wrong in the other direction, by claiming to know they have an account, would be the app asserting something it cannot know. My question to Christopher: none — his direction; the detection limit above is a constraint, not a choice.
Status: ratified 2026-07-26 (his direction), with the detection limit recorded.

**D-039** | Stage B | _Progress at three scales, a finish date derived from his own pace, and an animated node network behind the hero (Christopher's request, 2026-07-26)._
His request: a progress indicator per unit and for the whole project as well as per module, an estimated completion date from his average daily study time, gamified — and explicitly "do some research so you know how to get me to have dopamine hits so i actually want to do more". THE RESEARCH, done rather than assumed: across studies of gamified learning, a visible PROGRESS BAR is consistently the strongest single element for motivation, ahead of achievements, points and concept maps; goal-setting is the feature learners themselves rate most useful, and several near goals beat one distant goal; and self-determination theory explains the mechanism — what motivates is feedback signalling growing COMPETENCE, which a percentage of real work does and a badge does not. The literature also reports mixed and negative results for heavy gamification, which is the reason for what is NOT built here. DECIDED, therefore: progress at three scales (per lesson, per module, whole curriculum) and a projected finish date; NO points, badges, levels or leaderboards. A leaderboard needs other people and he is studying alone; badges reward the app's opinion rather than his competence. PER-LESSON PROGRESS reuses the D-035 reading positions, so a half-read lesson shows as half done instead of as untouched — the discouraging misreport the research warns against — and caps at 80% until the quiz is passed, because showing 100% for unfinished work would be false. THE FINISH DATE is computed in completion.ts from measured history and NOTHING ELSE: minutes per lesson from his own tracked time, minutes per active day, and study FREQUENCY (active days over elapsed days) as a separate factor, because assuming daily study would flatter the estimate while counting every elapsed day would punish a deliberate rest day. It returns null — and the app says so plainly — until two lessons and three study days exist, since a date that swings between sessions is the app guessing, and an invented date is worse than none because he would plan against it. THE ANIMATION replaces the static PCB trace with drifting nodes joined by lines as they come close, from a reference he sent. Canvas rather than SVG: forty nodes means hundreds of candidate lines per frame, which as SVG is hundreds of DOM nodes mutating sixty times a second. It stops entirely when scrolled off screen or the tab is hidden, caps device pixel ratio at 2, and draws a single still frame under prefers-reduced-motion. Palette stays copper rather than the reference's magenta, so it reads as this app's circuit-board identity rather than as a borrowed background. My question to Christopher: none — his direction.
CORRECTION, 2026-07-26, prompted by him asking whether the date updates when he spends more time in the app: it does not, and the first UI copy implied it did. Working the algebra: minutes per lesson is T/P and minutes per active day is T/A, so active days needed is R x (T/P) / (T/A) = R x A / P — total minutes CANCEL. Only finishing lessons and studying on more days move the projection. That is the right behaviour, since minutes spent without finishing anything are not evidence of progress, but "At 45 min a study day, finishing around September" read as though the minutes were driving it. The line now states the two things that actually do: lessons per study day, and days studied in seven.
Status: ratified 2026-07-26 (his direction).
